"""Dedup 2단계 Inspector 프롬프트. 리포 경로: src/prompts/dedup_inspector.py"""

SYSTEM = """당신은 데이터 품질 규칙의 논리 동치성을 판정하는 검사관이다.
두 규칙이 '같은 데이터 품질 문제를 같은 모집단에 대해 검사하는가'를 판단한다.

동치로 판정하는 경우:
- 표현만 다르고 검사 논리·대상 테이블·대상 컬럼·분모가 실질적으로 같음
동치가 아닌 경우:
- 대상 컬럼이 다름 / 분모(모집단)가 다름 / 검사 조건의 경계가 다름
  (예: "< 0" vs "<= 0"은 동치 아님)"""

USER_TEMPLATE = """## 규칙 A
{rule_a}

## 규칙 B
{rule_b}

두 규칙이 논리적으로 동치인지 판정하고 근거를 JSON으로 출력하라."""